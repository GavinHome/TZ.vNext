name: dingding

on: 
  push:
    branches:
    - master

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Send dingding notify
      uses: GavinHome/action-dingding@v1.0.5

